spring:
  profiles:
    active: test

  jpa:
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

logging:
  level:
    p6spy: info

decorator:
  datasource:
    p6spy:
      enable-logging: true